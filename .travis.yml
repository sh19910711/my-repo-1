language: bash

script:
  - export REPO=$PWD
  - git log --oneline
  - git clone github.com:sh19910711/my-repo-1.wiki /tmp/wiki
  - cd /tmp/wiki
  - git remote add repo $REPO
  - git fetch repo
  - git reset --hard repo/HEAD
  - git push origin master -f
